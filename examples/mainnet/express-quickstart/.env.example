# Server wallet — receives USDC payments (address only, no private key needed)
RECEIVING_ADDRESS=0xYourServerWalletAddress

# Client wallet — pays for API calls (private key required for test-client.ts)
CLIENT_PRIVATE_KEY=0xYourClientPrivateKey
