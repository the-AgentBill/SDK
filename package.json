{
  "name": "agent-bill",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --recursive run build",
    "dev": "pnpm --recursive run dev",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "MIT",
  "pnpm": {
    "patchedDependencies": {
      "@x402/express@2.6.0": "patches/@x402__express@2.6.0.patch"
    }
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "vitest": "^3.2.4"
  }
}
